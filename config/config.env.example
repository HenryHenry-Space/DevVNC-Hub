# Dev VNC Server 配置文件 / Configuration file
# 复制此文件到 ~/.config/dev-vnc/config.env 并根据需要修改 / Copy to ~/.config/dev-vnc/config.env and customize

# 显示器编号 (0-99) / Display number (0-99)
DEV_VNC_DISPLAY=99

# VNC 服务器端口 / VNC server port
DEV_VNC_PORT=5999

# noVNC Web 端口 / noVNC web port
DEV_VNC_NOVNC_PORT=6080

# 屏幕分辨率 (宽x高x色深) / Resolution (WxHxDepth)
DEV_VNC_RESOLUTION=1920x1080x24

# VNC 密码 / VNC password
DEV_VNC_PASSWORD=devvnc123

# 窗口管理器 (fluxbox, openbox, i3) / Window manager
DEV_VNC_WM=fluxbox

# 日志目录 / Log directory
DEV_VNC_LOG_DIR=$HOME/.dev-vnc/logs

# 运行时目录 / Runtime directory
DEV_VNC_RUN_DIR=$HOME/.dev-vnc/run
